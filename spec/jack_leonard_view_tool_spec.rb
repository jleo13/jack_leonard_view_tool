require "spec_helper"

describe JackLeonardViewTool do
  it "has a version number" do
    expect(JackLeonardViewTool::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
